# SPDX-FileCopyrightText: 2026 BFG-I contributors (HackUDC 2026)
# SPDX-License-Identifier: GPL-2.0-only

name: OpenSSF Scorecard

on:
  push:
    branches:
      - main
  schedule:
    - cron: '30 1 * * 1'   # Every Monday at 01:30 UTC
  workflow_dispatch:

# Read-only by default; specific jobs request what they need.
permissions: read-all

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write   # Upload SARIF to GitHub Security tab
      id-token: write          # Sign the results for publishing
      contents: read
      actions: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Run Scorecard
        uses: ossf/scorecard-action@v2.4.0
        with:
          results_file: results.sarif
          results_format: sarif
          # Publish to the public OpenSSF database (requires public repo).
          publish_results: true

      - name: Upload SARIF artifact
        uses: actions/upload-artifact@v4
        with:
          name: scorecard-sarif
          path: results.sarif
          retention-days: 5

      - name: Upload to GitHub code scanning
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
